

/////- на страницу альбома вывести название альбома
- в альбоме под фотку вывести названия, сделать возможность лайкать для авторизированных пользователей (1 пользователь 1 раз).
- там где выводится фотка, добавить вывод ее названия и описания
- желательно доделать возможность коментировать (для авторизированных) и возможность читать комменты (для гостей)
- сделать для юзера и для админа больше интересного функционала
- доделать универсальный css
- доделать js и php проверки авторизации и регистрации и остальных форм, обязательно применить функцию HTMLSpecialChars() - защиту от ввода html тегов
/////- разрешить использование цифр в пароле и логине
- сделать сервис "если забыли пароль" отправлять на почту по "вопрос/ответ"
- там где вводятся коментарии вместо инпутов сделать <textarea>
- добавить в записях значений в базу функцию addslashes() и функцию htmlspecialchars()
- добавить при выводе значений из базы stripslashes()


-----------------


Функция in_use() проверяет, существует ли уже в MySQL+базе вводимый пользователем идентификатор (userid), и возвращает 1, если это так:

	function in_use($userid)
	{
		global $user_tablename;
		$query = "SELECT userid FROM $user_tablename WHERE userid = '$userid'";
		$result = mysql_query($query);
			if(!mysql_num_rows($result)){
				return 0;
			}else{
				return 1;
			}
	}
